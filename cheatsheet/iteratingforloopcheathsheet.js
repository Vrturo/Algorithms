// for of statement 
// .length is for arrays
// number only is for arrays 
// for of loop
// for (let num of nums){
    // setting items in array to num 
    // this can be used for iterating through an array mainly 

// i is the loop variable
// initial expression; condition (how many times we want it to execute); increment expression(use incrememnt operator)
// for (let i = 0; i < variable.length; i++)
// this will keep running as long as the condition is true 
// this is common for strings and arrays 

// looping through key value pair
// for in loop 

// for in loop
// const user = {'firstName': 'John'}
// for (key in user)
// console.log(user[key])

// while loop
// declare index outside of loop
/**
 let i = 0
 while (i < 10) {
     console.log(i);
     i++;
 }
 */
// for each loop

 /**
  const animals = ['cat' , 'dog' , 'horses' , 'sheep' , 'bird'];
  animals.forEach( animal => {
      
  })
  */